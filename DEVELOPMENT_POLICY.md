# FogsGear 開発方針

## 画面構成

- ルートの `index.html` は互換入口とし、実際のゲーム入口は `MainSystem/index.html` とする。
- `MapSystem/` は探索マップ本体、`GearSystem/` はギア編集本体、`MainSystem/` はゲーム入口と共通の本体機能として責務を分ける。
- 共通アイコンとインベントリ用の画像は `MainSystem/icons/`、`MainSystem/icons/items/` に置く。
- 未作成の画像があっても、画面が壊れない代替表示を用意する。

## ポップアップ

- ポップアップは、画面の文脈を保ったまま情報や操作を重ねて表示する。
- ポップアップを別のポップアップから開いた場合、子ポップアップを閉じても親ポップアップは閉じない。
- 外側タップで閉じる処理は、最上位に表示されているポップアップだけに適用する。
- `pointerup` で外側タップを判定し、押し離し時の操作感を統一する。
- ポップアップ内のボタン操作は、背後のポップアップの外側タップ判定へ伝播させない。
- 閉じるボタンは本文と重ならないヘッダー行へ配置する。
- 長い名前や説明は省略せず、折り返して読めるようにする。

## UIデザイン

- スチームパンク世界観に合わせ、木炭色・真鍮色・青緑を基調にする。
- 地形情報など既存UIと操作感・枠線・余白の規則を揃える。
- 文字だけの操作対象ではなく、クリック可能な範囲が見た目で分かるボタンやカードにする。
- 画面外タップ、Escape、明示的な閉じる操作を用途に応じて用意する。
- モバイル幅でも文字や操作ボタンが重ならないことを実画面で確認する。

## 実装と確認

- 既存の状態管理、イベント委譲、ファイル構成を優先して使う。
- 変更後は対象ファイルの診断を実行する。
- 見た目を変更したときは、キャッシュを避けて実画面を再読込し、スクリーンショットで確認する。
- 既存の保存データやLocalStorageのキーを、理由なく変更・削除しない。
